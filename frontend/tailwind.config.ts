import type { Config } from 'tailwindcss';

// Tailwind v4 is CSS-first (see app/globals.css's @theme block) and
// auto-detects content — this file is kept only because components.json
// references it, and to leave a place for future JS-level config if needed.
const config: Config = {};

export default config;
